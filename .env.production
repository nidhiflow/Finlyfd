VITE_API_BASE_URL=https://api.nidhiflow.in

# Must match PAYMENTS_ENABLED in the backend .env. Flip to true together with the
# backend flag once real Razorpay keys are live.
VITE_PAYMENTS_ENABLED=false

# TODO before shipping a real build: swap for your LIVE Razorpay key (rzp_live_...).
# This is a fallback only — checkout normally uses the key_id returned by
# /api/payments/create-order, so the backend's RAZORPAY_KEY_ID on Render matters more.
# Still currently set to a TEST key below (copied from local dev):
VITE_RAZORPAY_KEY_ID=rzp_test_TMnF3pCsxlo1Is

VITE_GOOGLE_CLIENT_ID=819893197591-41ich6canmvc66tsrc8cgn17t17cq1hv.apps.googleusercontent.com
